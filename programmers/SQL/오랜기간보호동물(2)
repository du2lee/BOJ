SELECT INS.ANIMAL_ID, INS.NAME FROM ANIMAL_INS as INS LEFT OUTER JOIN ANIMAL_OUTS as OUTS ON INS.ANIMAL_ID = OUTS.ANIMAL_ID WHERE OUTS.DATETIME IS NOT NULL ORDER BY (OUTS.DATETIME - INS.DATETIME) DESC LIMIT 2
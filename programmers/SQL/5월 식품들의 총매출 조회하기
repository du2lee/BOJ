SELECT A.PRODUCT_ID, PRODUCT_NAME, SUM(AMOUNT * PRICE) as TOTAL_SALES FROM FOOD_PRODUCT as A JOIN FOOD_ORDER as B ON A.PRODUCT_ID = B.PRODUCT_ID WHERE MONTH(B.PRODUCE_DATE) = 5 GROUP BY PRODUCT_NAME ORDER BY TOTAL_SALES DESC
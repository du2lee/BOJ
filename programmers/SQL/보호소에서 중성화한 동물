SELECT INS.ANIMAL_ID, INS.ANIMAL_TYPE, INS.NAME FROM ANIMAL_INS as INS INNER JOIN ANIMAL_OUTS as OUTS ON INS.ANIMAL_ID = OUTS.ANIMAL_ID WHERE NOT INS.SEX_UPON_INTAKE = OUTS.SEX_UPON_OUTCOME ORDER BY INS.ANIMAL_ID ASC